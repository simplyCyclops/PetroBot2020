package runs;

import robot.utils.RobotRun;

/**
 * Sample robot run
 * @author John
 */
public class SampleRun extends RobotRun{

	@Override
	public void runInstructions() {
		/* RobotMap.getInstance().sampleLeftMotor.forward();
		 * Wait.waitForSeconds(3);
		 * RobotMap.getInstance().sampleLeftMotor.stop(); */
	}
}
